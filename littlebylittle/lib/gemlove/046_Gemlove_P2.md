## install rspec
* create Gemfile and run command
   ```sh
bundle install --binstubs

   ```
* install rspec

   ```sh
   bin/rspec --init
   # => create   .rspec
   #=> create   spec/spec_helper.rb
   ```
* run rspec

   ```sh
    rspec
   ```
